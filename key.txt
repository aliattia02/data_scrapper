# Backend
cd data_scrapper
pip install -r requirements. txt
python -m src.main db init    # Not --init
python -m src.main db seed    # Not --seed (though this might not exist)
python -m src.main serve          # Start API on :8000

# Frontend (separate terminal)
cd frontend
npm install
npm run dev                        # Start on :5173

# Or with Docker
docker-compose up